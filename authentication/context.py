# # context.py (create this module if it doesn't exist)
# from django.contrib.auth.middleware import get_user

# class MyGraphQLContext:
#     def __init__(self, request):
#         # Use Django's get_user function to retrieve the user based on the request
#         self.user = get_user(request)
